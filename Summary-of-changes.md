Goals:
- Minimize the places things are done.
- Each thing only done in one place.
- Remove dependence on ffmpeg (binary problems). Only use python
  packages. (It's okay if the package as a binary component, but it
  must be parted of the pypi package.)
