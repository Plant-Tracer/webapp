env:
  browser: true,
  jquery: true,
  es2021: true

extends: eslint:recommended

ignorePatterns: ["unzipit-worker.module.js"]

parserOptions:
  ecmaVersion: latest

rules:
  "no-unused-vars": ["error", { "args": "all", "argsIgnorePattern" : "^_" }]
